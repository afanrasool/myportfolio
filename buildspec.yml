version: 0.1

phases:
  build:
    commands:
      - npm install
      - npm run webpack
artifacts:
    files:
      - index.html
      - favicon.ico
      - notesonAutomation.txt
      - styles/*
      - images/*
      - githelp.txt
      - dist/bundle.js
